apiVersion: v1
kind: Service
metadata:
  name: qbittorrent-exporter
  namespace: media
spec:
  ports:
    - port: 8000
      targetPort: 8000
      name: metrics
  selector:
    app: qbittorrent-exporter
